# SwiftPM build products
/.build/
.swiftpm/

# Assembled/signed .app bundle produced by Scripts/build.sh, and the
# release zips Scripts/release.sh uploads to GitHub Releases.
/build/

# An exported Sparkle private key must never be committed — it is the only
# thing standing between a user and a forged "update".
cmdv-sparkle-key.txt
*sparkle*key*.txt

# Xcode
xcuserdata/
DerivedData/
*.xcuserstate

# macOS
.DS_Store
